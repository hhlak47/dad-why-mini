// ============================================================
// 小程序配置文件模板
// API Key 永远不要写在前端代码里，只存在于云端（云函数环境变量）
// 使用时复制为项目中的 config.js 并填入你自己的值
// ============================================================

module.exports = {
  // 后端模式，三选一：
  //   'mock'  —— 本地假数据，无需任何账号/Key，先看界面与交互流程
  //   'cloud' —— 微信云函数（推荐，需真实 AppID + 云环境 + API Key）
  //   'http'  —— 自建服务器（server/index.js，需 Node + API Key，手机与电脑同 WiFi）
  BACKEND_MODE: 'cloud',

  // ===== 云函数模式 =====
  // 在微信开发者工具 → 云开发控制台 → 设置 中获取环境 ID
  CLOUD_ENV: 'your-cloud-env-id',
  CLOUD_FUNCTION: 'ask',           // AI 问答云函数名
  CLOUD_VOICE_FUNCTION: 'voice',   // 语音识别/合成/克隆云函数名

  // ===== 语音音色 =====
  // 默认音色：未做声音克隆时，「念给孩子听」使用的预设音色
  // 使用基础 TTS 数字 ID（如 101001 智瑜·温柔女声），0.3 元/万字符
  // 克隆音色（v-xxx 格式）自动走 TRTC TTS，3 元/万字符
  // 基础 TTS 精品音色可选：101001 智瑜 / 101002 智聆 / 101003 智美 / 101004 智云 / 101005 智莉
  DEFAULT_VOICE_ID: '101001',
  // 父母克隆出的音色存在云数据库 voices 集合（role: dad / mom），运行时按所选角色读取

  // ===== HTTP 模式（仅当 BACKEND_MODE='http' 时使用）=====
  // 填你电脑在当前 WiFi 下的局域网 IP，例如 http://192.168.1.100:3000
  // 注意：WiFi 重连后 IP 可能变化，需重新查看并修改
  HTTP_BASE_URL: 'http://192.168.1.100:3000',
  VOICE_BASE_URL: 'http://192.168.1.100:3000'
}
