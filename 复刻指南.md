# 《爸爸，我为什么？》复刻指南

> 这份指南写给**第一次拿到代码的你**。照着一步步做，大约 1～2 小时可以让小程序在你自己的手机上跑起来，并且能用**你自己的声音**念 AI 给孩子的回答。
>
> 不需要懂编程，会装软件、会复制粘贴即可。

---

## 一、你需要准备的东西

| 项目 | 用来干什么 | 在哪弄 | 费用 |
|------|-----------|--------|------|
| **微信开发者工具** | 打开和运行小程序代码 | 微信公众平台官网下载（稳定版） | 免费 |
| **微信小程序账号**（个人即可） | 得到你自己的 AppID，用来真机预览 | mp.weixin.qq.com 注册，个人邮箱免费注册 | 免费 |
| **DeepSeek API Key** | AI 生成回答 | platform.deepseek.com 注册后在「API keys」创建 | 充值几块钱够用很久 |
| **腾讯云账号**（可选，要声音克隆才需要） | 语音识别、语音合成、声音克隆 | cloud.tencent.com 实名认证 | ASR/TTS 很便宜，克隆约 3 元/万字 |

> 💡 **只想先看看界面**：上面一个 Key 都不用申请，先按第六节用 `mock` 模式跑起来。
> 💡 **要 AI 文字问答 + 普通声音播放**：只需小程序账号 + DeepSeek Key + 腾讯云 ASR/TTS。
> 💡 **要克隆爸爸妈妈自己的声音**：上面全部 + 腾讯云开通 TRTC。

---

## 二、拿到代码

在你要放代码的文件夹里，打开「终端」执行：

```bash
git clone https://github.com/hhlak47/dad-why-mini.git
```

（如果没装 git，也可以直接在 GitHub 页面点绿色的 **Code → Download ZIP**，解压即可。）

---

## 三、导入项目并换成你自己的 AppID

1. 打开**微信开发者工具** → 「导入项目」
2. 目录选择刚才下载的 `dad-why-mini` 文件夹
3. **AppID 填你自己的**（在微信公众平台 → 开发管理 → 开发设置里能看到，格式形如 `wxXXXXXXXX`）
   - ⚠️ 不要用代码里自带的 `wxda7c14cfae9e55df`，那是作者的，你没有权限
4. 导入后，用编辑器打开 `project.config.json`，确认第 34 行 `"appid"` 已经是你的 AppID（工具导入时一般会自动改好）

---

## 四、开通微信云开发

1. 在微信开发者工具顶部，点「**云开发**」按钮 → 开通（个人免费）
2. 创建一个环境，名字随意，比如 `my-why`
3. 创建后，在「云开发控制台 → 设置」里复制**环境 ID**（形如 `cloud1-xxxxxxxx`）
4. 打开代码里的 **`config.js`**，把第 14 行改成你的环境 ID：

```js
BACKEND_MODE: 'cloud',
CLOUD_ENV: 'cloud1-这里换成你的环境ID',
```

> ⚠️ 代码里自带的 `cloud1-d8gxvnxs47a96f2b3` 是作者的环境，你访问不了，必须改成自己的。

---

## 五、部署云函数（关键）

项目有两个云函数：`ask`（AI 问答）和 `voice`（语音）。

1. 在开发者工具左侧文件树找到 `cloudfunctions/` 文件夹
   - 如果 `cloudfunctions` 图标是灰色或没有小云朵，右键它 → 「同步云函数列表」/ 确认环境
2. **分别右键 `cloudfunctions/ask` 和 `cloudfunctions/voice` 文件夹**
3. 选择「**上传并部署：云端安装依赖（不上传 node_modules）**」
4. 等待提示「上传成功」（voice 云函数只有 11KB，几秒就好；如果卡住，等 1-2 分钟，**不要重复狂点**）
5. 在云开发控制台 → 云函数，确认 `ask` 和 `voice` 都出现在列表里

### 配置云函数的「密钥环境变量」

在**云开发控制台 → 云函数**里：

**点 `ask` → 配置 → 环境变量**，添加：

| Key | Value |
|-----|-------|
| `API_KEY` | 你的 DeepSeek API Key（形如 `sk-xxxxxxxx`） |
| `API_BASE` | `https://api.deepseek.com/v1`（用 DeepSeek 可省略不填） |
| `MODEL` | `deepseek-chat`（可省略不填） |

**点 `voice` → 配置 → 环境变量**，添加（要语音功能才需要）：

| Key | Value |
|-----|-------|
| `TENCENT_SECRET_ID` | 腾讯云 SecretId |
| `TENCENT_SECRET_KEY` | 腾讯云 SecretKey |
| `TRTC_SDK_APP_ID` | TRTC 应用的 SdkAppId（纯数字，**不是**腾讯云 AppID） |

> 腾讯云密钥在 [console.cloud.tencent.com/cam/capi](https://console.cloud.tencent.com/cam/capi) 创建。
> TRTC SdkAppId 在 [TRTC 控制台](https://console.cloud.tencent.com/trtc) 创建应用后获取。

### 设置云函数超时时间

- `ask` 云函数：超时设为 **20 秒**（默认 3 秒会超时报错 -504003）
- `voice` 云函数：超时设为 **60 秒**（声音克隆比较慢）
- 位置：云开发控制台 → 云函数 → 点函数名 → 配置 → 超时时间
- （代码里 `cloudfunctions/*/config.json` 已写好，重新部署一般会带上，若不生效就到控制台手动改）

---

## 六、先跑通界面（mock 模式，零配置）

如果暂时不想配 Key，先验证界面：

1. 打开 `config.js`，把 `BACKEND_MODE` 改成 `'mock'`
2. 点开发者工具的「编译」，首页就能输入问题、看到示例回答、走通所有页面
3. 看够了再改回 `'cloud'` 配真实 Key

---

## 七、创建数据库集合（要声音克隆才需要）

1. 云开发控制台 → 数据库 → 「+ 新建集合」，名字填 **`voices`**
2. 点进 `voices` 集合 → 「权限设置」
3. 选「**所有用户可读，仅创建者可读写**」（开发期最省事）
   - ⚠️ 不设权限的话，克隆成功后保存会报权限错误

---

## 八、开通腾讯云语音服务（要语音/克隆才需要）

登录腾讯云控制台，确认开通：
- **语音识别 ASR**（录音转文字）
- **语音合成 TTS**（文字转声音）
- **实时音视频 TRTC**（声音克隆 VoiceClone 用，含克隆音色播放）

首次使用按提示开通并实名认证即可，有免费试用额度。

---

## 九、测试流程

在开发者工具里点「预览」用手机微信扫码，按这个顺序测：

1. **文字问答**：首页输入问题 → 看到 AI 回答 ✅
2. **语音输入**：首页按住麦克风说话 → 文字出现在输入框
3. **普通播放**：点「🔊 念给孩子听」→ 用默认女声播放（iPhone 注意静音键要关，代码已处理）
4. **声音克隆**：进「我的声音」→ 选爸爸/妈妈 → 按住读 15～30 秒引导文字 → 提交
   - 成功后聊天页播放按钮旁会出现「爸爸/妈妈」音色选项
5. **收藏**：点回答卡片的「☆ 收藏」→ 首页「我的收藏」里能看到

---

## 十、常见问题（踩坑速查）

| 现象 | 原因 / 解决 |
|------|------------|
| 云函数报 `-504002 functions execute fail` | 云函数没部署成功，或超时太短。重新「上传并部署」，超时按第五节设置 |
| 云函数报 `-504003` 超时 | ask 设 20 秒、voice 设 60 秒 |
| 部署卡在 26% 或报 `Updating 状态` | 等 1～2 分钟再点一次，别狂点 |
| 语音提示「data exceed max size」 | 录音格式问题，代码已用 mp3，若自己改过录音配置，改回 `format:'mp3'` |
| 克隆失败 `SdkAppIdNotExist` | `TRTC_SDK_APP_ID` 填错了，要用 TRTC 应用的 SdkAppId，不是腾讯云 AppID |
| 克隆失败 `AuthFailure` | `TENCENT_SECRET_ID/KEY` 错了，或没开通对应服务 |
| 克隆保存时报权限错误 | 数据库 `voices` 集合没设权限，看第七节 |
| iPhone 播放没声音 | 静音键关了即可（代码已设 `obeyMuteSwitch=false`） |
| 改了环境变量不生效 | 环境变量在**云开发控制台**改，改完重新调用即可（不用重新部署代码） |
| 想换成别的 AI（混元/OpenAI 等） | 只要是 OpenAI 兼容接口，改 ask 云函数的 `API_BASE` 和 `MODEL` 环境变量即可 |

---

## 十一、想做成你自己的版本？

仓库里附带了一份 **Skill 构建指南**（`.trae/skills/kids-ai-voice-app/`），里面沉淀了这套小程序的完整架构、云函数代码模板和踩坑经验。如果你用 **TRAE** 这类 AI 编程工具，直接对它说：

> 「帮我做一个亲子 AI 语音问答小程序，参考 kids-ai-voice-app 这个 Skill」

它就能照着指南帮你生成一套属于你自己的、可以自由改名字/换风格/加功能的小程序。详见：
- `.trae/skills/kids-ai-voice-app/SKILL.md`（总指南）
- `.trae/skills/kids-ai-voice-app/frontend-contracts.md`（前端数据结构）
- `.trae/skills/kids-ai-voice-app/templates/`（可复用代码模板）

---

祝你的孩子永远有问不完的「为什么」，而你永远有答案。
