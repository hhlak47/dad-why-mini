# future.md —— 暂未实现、留待后续的功能

> 按 PRD §30「禁止主动扩展需求」原则，以下功能在 v0.1 阶段**不实现**，仅记录，便于后续决策。

## P1 中本次未实现
- **本地保存最近 10 个问题**：需增加历史列表页与本地存储结构，可能引入「历史」心智，与“每屏一个核心任务”略有冲突，留待确认价值后再做。

## 已实现（曾列为后续）
- **语音输入（按住说话）转文字** 与 **语音播放（念给孩子听）**：已在 v0.1.1 实现，基于腾讯云 ASR/TTS，密钥只存服务器端；前端 `utils/voice.js` + 首页「🎤 按住说话」+ 回答卡片「🔊 念给孩子听」。详见 README「六·二、语音功能」。

## 明确不做的（PRD §5 明确不做）
微信登录、手机号注册、云端用户体系、会员、支付、收藏夹、孩子成长档案、多孩子管理、AI 绘本、AI 生图、AI 音频故事、语音克隆、分享海报、社区、排行榜、复杂后台、内容运营系统、数据分析后台、历史记录（除上面 P1 可选外）、多孩子、后台管理。

## 可能的下一阶段增强（需先验证 MVP 核心假设）
- 追问轮数动态上限（目前硬编码 5 轮）。
- 「讲给孩子听」多语言 / 方言念读（TTS）。
- 父母反馈闭环：对每个回答打「直接念 / 稍改 / 不想念」，用于改进 System Prompt。
- 游戏分类标签，便于父母按「找一找/演一演」等主动挑选。
- 敏感问题（健康/安全）命中时，前端额外展示「建议咨询专业人员」提示条。

## 技术债 / 待优化
- System Prompt 在 `cloudfunctions/ask/index.js` 与 `server/index.js` 中各有一份拷贝，修改需同步；可改成统一引用一份文件以减少漂移。
- 目前未接埋点（PRD §25 建议在控制台记录 question_submit / answer_generated 等事件），正式测试前可补一个轻量 logger。
